# DataSource settings: set here your own configurations for the database
# connection. In this example we have "netgloo_blog" as database name and
# "root" as username and password.
#spring.datasource.url = ${OPENSHIFT_MYSQL_DB_HOST}:${OPENSHIFT_MYSQL_DB_PORT}/"nameofDB"
# spring.datasource.username = ${OPENSHIFT_MYSQL_DB_USERNAME}
# spring.datasource.password = ${OPENSHIFT_MYSQL_DB_PORT}
spring:
  datasource:
    url: ${OPENSHIFT_MYSQL_DB_HOST}:${OPENSHIFT_MYSQL_DB_PORT}/pinetdev?createDatabaseIfNotExist=true
    username:  ${OPENSHIFT_MYSQL_DB_USERNAME}
    password: system
    testWhileIdle: ${OPENSHIFT_MYSQL_DB_PASSWORD}
    validationQuery: SELECT 1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
      dialect: org.hibernate.dialect.MySQL5Dialect
  h2:
    console:
      enabled: true

  output:
    ansi:
        enabled: ALWAYS

  thymeleaf:
    cache: false

#messaging
  mail:
    default-encoding: UTF-8
    host: smtp.gmail.com
    username: tiwaryashmeet@gmail.com
    password: Pokhara98254
    port: 587
#    host: mailrelay.intranet.hdp.be
#    username: test
#    password: test
#    port:
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            trust: smtp.gmail.com
    protocol: smtp
    test-connection: false



liquibase:
  change-log: classpath:db/changelog/changelog.xml

#spring.output.ansi.enabled=always
#spring.main.banner-mode=off
#logging
logging:
    file: src/main/resources/logs/application.log
    pattern:
        console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
        file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"


