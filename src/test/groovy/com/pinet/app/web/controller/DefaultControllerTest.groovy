package com.pinet.app.web.controller

import org.springframework.web.client.RestTemplate
import spock.lang.Specification

class DefaultControllerTest extends Specification {
    def "TestMessage"() {
    }

    def "Simple status checker"() {
//        when: "a rest call is performed to the status page"
//        RestTemplate restTemplate = new RestTemplate()
//        String status = restTemplate.getForObject("http://localhost:8080/test", String.class)
//
//        then: "the correct message is expected"
//        status == "Test"
    }
}
